#include <stdio.h>

int main(){
    char s[501];
    gets(s);

    int len,i,count=0;
    
    // for(len=0; s[len]!='\0'; len++){

    // }
    for(len=0; s[len]!=0; len++){

    }
    printf("%d",len);


    return 0;
}